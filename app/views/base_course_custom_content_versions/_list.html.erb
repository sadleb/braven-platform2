<div class="row">
  <div class="col-sm-6">
    <table class="table table-sm table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% base_course_custom_content_versions.each do |bcccv| %>
          <tr>
            <td><%= link_to bcccv.custom_content_version.title, bcccv.canvas_url %></td>
            <td class="text-right smaller">
              <% unless local_assigns[:read_only]  %>
                <%= link_to 'Publish Latest', base_course_base_course_custom_content_version_path(bcccv.base_course_id, bcccv.id), method: :put %> |
                <%= link_to 'Delete', base_course_base_course_custom_content_version_path(bcccv.base_course_id, bcccv.id), method: :delete,
                                      data: { confirm: 'Are you sure you want to delete this item?' } %> 
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
