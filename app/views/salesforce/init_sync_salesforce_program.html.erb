<% content_for(:headline) do %>
 Sync From Salesforce
<% end %>

<div class="col">

  <div class="row">
    <div class="col-md-4">
      <%= form_with(url: salesforce_sync_salesforce_program_path, method: "post", local: true) do |f| %>
        <div class="form-group">
          <%= f.label :program_id, "Program ID:" %>
          <%= f.text_field :program_id, value: params[:program_id], autofocus: true, class: "form-control", required: true, placeholder: "a34scx992...", autocomplete: "salesforce-program-id" %>
        </div>

        <div class="form-group">
          <%= f.label :email, "Email to notify when sync completes:" %>
          <%= f.email_field :email, value: params[:email], class: "form-control", required: true, placeholder: "you@example.com", autocomplete: "email" %>
        </div>

        <div class="form-check mt-2">
          <%= f.check_box :force_zoom_update, { class: "form-check-input" }, "true", "false" %>
          <%= f.label :force_zoom_update, "Force Zoom links to be updated?", class: "form-check-label" %>
        </div>

        <br>

        <div class="actions">
          <%= f.submit "Start Sync From Salesforce Process" %>
        </div>
      <% end %>
    </div>
  </div>

  <p><small><em>Notes:</em>
    <ul>
      <li><p>The "Force Zoom links ..." option should rarely need to be used. It's for when the meeting password or something else core to the meeting causes the links to become invalid even if nothing about the Participants have changed. Note that if their name or Zoom prefix changes that will update in Zoom but the link will still be the same.</p></li>
      <li><p>If you're scrambling to get a last minute Participant access without waiting for the normal nightly sync and Campaign Monitor "welcome email" process, you can send them a sign-up email from their User page. E.g. imagine you're in-person at the first learning lab with a Fellow not already setup and you're trying to get them access.</p></li>
    </ul>
  </small></p>

  <hr>

</div>
