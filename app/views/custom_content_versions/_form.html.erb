<%= form_with(model: custom_content_version, local: true) do |form| %>
  <% if custom_content_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_content_version.errors.count, "error") %> prohibited this custom_content_version from being saved:</h2>

      <ul>
        <% custom_content_version.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :custom_content_id %>
    <%= form.text_field :custom_content_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
