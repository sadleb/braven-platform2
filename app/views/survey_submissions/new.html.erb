<%= form_for [@base_course_custom_content_version, @survey_submission] do |f| %>
  <div class="bz-assignment">
    <%== @base_course_custom_content_version.custom_content_version.body %>
    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
  </div>
<% end %>
