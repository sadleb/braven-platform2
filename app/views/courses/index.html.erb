<div class="container">
<%= render(
  'list',
  name: 'Course Template',
  button_label: 'Launch New Program',
  button_url: launch_courses_path,
  courses: @courses.unlaunched_courses,
) %>

<%= render(
  'list',
  name: 'Launched Course',
  courses: @courses.launched_courses,
  button_label: nil,
) %>

</div>
